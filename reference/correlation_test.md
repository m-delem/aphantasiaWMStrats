# Spearman correlation with its sample size

Spearman correlation with its sample size

## Usage

``` r
correlation_test(x, y)
```

## Arguments

- x, y:

  Numeric vectors.

## Value

A one-row tibble with `rho`, `p` and `n`.
