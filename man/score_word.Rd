% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_scores.R
\name{score_word}
\alias{score_word}
\title{Word-recall similarity (Gonthier 2022 edit-distance scoring)}
\usage{
score_word(target, response)
}
\arguments{
\item{target, response}{Character vectors of equal length.}
}
\value{
Numeric vector in [0, 1].
}
\description{
\code{similarity = (nchar(target) - min(DL, nchar(target))) / nchar(target)},
where \code{DL} is the Damerau-Levenshtein distance between the normalised
target and response. Bounded [0, 1] and floored at 0 by construction:
the cap on \code{DL} before subtraction prevents a negative score when the
response is longer than the target.
}
\details{
Two deliberate departures from the task's live feedback, both recorded
in \code{01-score-computation.md} §2: the denominator is \code{nchar(target)}
alone rather than \code{max(nchar(target), nchar(response))}, and the metric
is Damerau-Levenshtein rather than plain Levenshtein. Note that the
live-feedback column \code{live_diff_word} is not a valid edit distance at
all, so this is not a refinement of it — see \link{all_data}.

\strong{Scope caveat.} Gonthier's method is developed and validated for
serial span tasks, where a trial's target and response are each a
sequence of several stimuli encoded as one string. WM-FTT compares one
target word to one recalled word. The distance computation transfers
directly; the validation does not cover this exact use.

An empty response scores 0 by construction, which is why non-responses
need no special case for this feature (unlike colour — see
\code{\link[=compute_scores]{compute_scores()}}).
}
