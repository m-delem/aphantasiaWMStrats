% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelling_tools.R
\name{fit_brms_model}
\alias{fit_brms_model}
\title{Fit a Bayesian model using the brms package with default settings}
\usage{
fit_brms_model(
  ...,
  chains = 4,
  iterations = 2000,
  warmup = 1000,
  cores = chains,
  refresh = 500,
  backend = "rstan",
  file_refit = "on_change",
  file_compress = "xz",
  sample_prior = FALSE,
  save_pars = NULL,
  adapt_delta = 0.95,
  max_treedepth = 10,
  seed = 667
)
}
\arguments{
\item{...}{Arguments passed to \code{brms::brm()}, such as formula, data,
family, priors, file.}

\item{chains}{Number of MCMC chains. Default 4.}

\item{iterations}{Post-warmup iterations per chain. Default 2000.}

\item{warmup}{Warmup iterations per chain. Default 1000.}

\item{cores}{Cores for parallel processing. Default \code{chains}.}

\item{refresh}{Frequency of progress updates. Default 500.}

\item{backend}{Backend for fitting. Default "rstan" (cmdstanr conflicts
with pkgdown).}

\item{file_refit}{Condition for refitting a cached model. Default
"on_change".}

\item{file_compress}{Compression for the saved model. Default "xz".}

\item{sample_prior}{Whether to draw prior samples. Default FALSE.}

\item{save_pars}{Parameters to save. Default NULL.}

\item{adapt_delta}{Target acceptance rate. Default 0.95.}

\item{max_treedepth}{Maximum treedepth. Default 10.}

\item{seed}{Random seed. Default 667.}
}
\value{
A fitted brms model object.
}
\description{
Ported from \code{aphantasiaEmotions} so both packages fit models the same way.
\code{iterations} is post-warmup draws \strong{per chain}, not a total.
}
