% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_results.R
\name{plot_split_half}
\alias{plot_split_half}
\title{Split-half reliability, per feature}
\usage{
plot_split_half(
  reliability,
  threshold = 0.7,
  labels = c(word = "Word", angle = "Orientation", color = "Colour"),
  base_size = 7
)
}
\arguments{
\item{reliability}{A named list or data frame of corrected correlations,
one element or column per feature. Names should be the feature stems
(\code{word}, \code{angle}, \code{color}) or their display labels.}

\item{threshold}{Where to draw the reference line. 0.70 is the
conventional floor for group-level use, 0.80 for individual-level.}

\item{labels}{Named vector mapping feature stems to display labels.}

\item{base_size}{Passed to \code{\link[=theme_pdf]{theme_pdf()}}. Scripts use the 7pt default;
pkgdown pages use 16.}
}
\value{
A ggplot object.
}
\description{
The distribution of Spearman-Brown corrected split-half correlations
across repeated random splits, with the conventional 0.70 floor marked.

Repeated splits rather than one arbitrary odd-even partition, and splits
drawn over trials rather than items, because items within a trial share
an encoding episode. See \code{\link[=split_half_reliability]{split_half_reliability()}}.
}
