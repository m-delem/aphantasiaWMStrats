% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelling_spec.R
\name{response_priors}
\alias{response_priors}
\title{Coefficient-wise priors for one response}
\usage{
response_priors(response, terms = c("vviq", "complete_aphant", "parity_rate"))
}
\arguments{
\item{response}{The brms response name, with separators stripped. Pass
\code{NULL} for a univariate model, where coefficients belong to no
response and \code{resp} must be omitted rather than empty.}

\item{terms}{Which coefficients the model contains. Priors are only set
for terms that are present, since naming an absent coefficient makes
brms reject the whole call.}
}
\value{
A \code{brmsprior} object.
}
\description{
Priors are set per coefficient rather than per class, because a VVIQ
slope acts over a 64-point range and a binary group offset does not,
and on the logit scale a single \code{class = "b"} prior would treat them as
the same quantity.

In a multivariate model, population-level coefficients belong to a
response, so \code{resp} is required: \code{class = "b"} alone matches no
parameter and recent brms versions reject it outright. Note that brms
strips non-alphanumeric characters from response names, so the argument
here is \code{"scoreword"}, not \code{"score_word"}.
}
