% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_composition.R
\name{plot_composition_ternary}
\alias{plot_composition_ternary}
\title{Ternary diagram of the three-part WM-FTT composition}
\usage{
plot_composition_ternary(
  parts,
  group = NULL,
  center = TRUE,
  scale = TRUE,
  labels = c("Words", "Orientations", "Colours"),
  point_size = 1.2,
  point_alpha = 0.8,
  base_size = 7,
  ...
)
}
\arguments{
\item{parts}{A data frame with \code{part_word}, \code{part_angle}, \code{part_color}.}

\item{group}{Optional vector of group labels, the same length as
\code{nrow(parts)}, used for colour and shape.}

\item{center, scale}{Passed to \code{coda.plot::ternary_frame()}. Both default
to \code{TRUE}: the composition varies over a very narrow range (parts have
SDs near 0.025), so an uncentred ternary is a single dot in the middle
of a large empty triangle. Centring and scaling zoom on the actual
spread, and the caption says so.}

\item{labels}{Corner labels, in the column order word, orientation, colour.}

\item{point_size, point_alpha}{Point aesthetics.}

\item{base_size}{Base font size passed to \code{\link[=theme_pdf]{theme_pdf()}}.}

\item{...}{Additional arguments passed to \code{\link[=theme_pdf]{theme_pdf()}}.}
}
\value{
A ggplot object.
}
\description{
Visualises the raw composition, which is a different representation from
the ILR coordinates the models are fitted on, not a competing one
(\code{06-compositional-analysis.md} §1). The ternary plot is the intuitive
three-way picture; the inference happens on log-ratios.

Built from \code{coda.plot}'s primitives (\code{ternary_frame()}, \code{ternary_plot()},
\code{ternary_coords()}) rather than from \code{coda.plot::ternary_diagram()}. The
legacy \code{plot_wm_composition()} in the waiting-room used the all-in-one
function and then reached into \code{p$layers[[i]]$geom$default_aes} to restyle
it, which breaks silently whenever ggplot2 changes its internals. Drawing
the points here means the aesthetics are ours and go through the public
API.
}
