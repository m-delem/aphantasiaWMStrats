% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggplot_tools.R
\name{save_ggplot}
\alias{save_ggplot}
\title{Custom ggsave wrapper set with Nature's formatting guidelines (width-locked)}
\usage{
save_ggplot(
  path,
  plot = ggplot2::get_last_plot(),
  ncol = 1,
  width = NULL,
  height = 90,
  return = FALSE,
  verbose = TRUE,
  units = "mm",
  dpi = 600,
  ...
)
}
\arguments{
\item{path}{A character string with the path to save the plot.}

\item{plot}{The ggplot object to save, defaults to last plot displayed.}

\item{ncol}{The number of columns for the plot. Either 1 (default) or 2.}

\item{width}{Optional. The width of the plot in mm. If NULL (default), it
will be set to 88 mm for one-column figures and 180 mm for
two-column figures.}

\item{height}{The height of the plot in mm. Default is 90 mm.}

\item{return}{Logical. Whether to return the plot visibly or not.}

\item{verbose}{Logical. Whether to print a message when saving is done.}

\item{units}{The units for the width and height. Default is "mm".}

\item{dpi}{The resolution of the plot. Default is 600 (irrelevant for
vector devices, kept so the same call works for PNG).}

\item{...}{Additional arguments passed to \code{ggsave()}.}
}
\value{
The plot, invisibly. Called for the side effect of saving.
}
\description{
Ported from \code{aphantasiaEmotions}. A one-column figure is 88 mm wide and a
two-column figure is 180 mm wide. Widths are locked to those two values
unless \code{width} is given explicitly, since the whole point is that figures
are designed at their final printed size rather than scaled afterwards.
}
