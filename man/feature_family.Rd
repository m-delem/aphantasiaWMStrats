% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelling_spec.R
\name{feature_family}
\alias{feature_family}
\title{Response families used for each recall feature}
\usage{
feature_family(feature = c("word", "angle", "color"))
}
\arguments{
\item{feature}{One of \code{"word"}, \code{"angle"}, \code{"color"}.}
}
\value{
A brms family object.
}
\description{
The three features need three different families, and each choice
follows from what the boundary of that feature's scale means rather
than from convenience.
\itemize{
\item \strong{Word: zero-one-inflated Beta.} About 91\% of responded word items
score exactly 1, because a recalled word either matches the target or
does not. That is a genuine point mass and earns an inflation
component, as do the 3\% scoring exactly 0.
\item \strong{Orientation: Beta.} No boundary mass at all once non-responders are
removed.
\item \strong{Colour: Beta.} Boundary mass exists but is negligible and is an
artifact: colour score is cosine similarity on a continuous wheel, so
an exact 1 is an error of exactly zero degrees, which is pixel
resolution rather than a behaviour. Handle it with
\code{\link[=squeeze_boundaries]{squeeze_boundaries()}} rather than an inflation component.
}
}
