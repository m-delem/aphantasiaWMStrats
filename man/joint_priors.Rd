% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modelling_spec.R
\name{joint_priors}
\alias{joint_priors}
\title{Priors for the joint model}
\usage{
joint_priors(
  parity = TRUE,
  lkj = 4,
  features = c("word", "angle", "color"),
  accuracy_features = features
)
}
\arguments{
\item{parity}{Whether to include \code{parity_rate} on the accuracy arms.}

\item{lkj}{The LKJ shape parameter for the correlation matrix.}

\item{features}{Feature stems, in the order the responses should appear.
Gates are built for all of them.}

\item{accuracy_features}{Which features get an accuracy arm. Defaults to
all of them. Pass \code{character(0)} for the gates alone, which is the
response-propensity model in its own right, or drop \code{"word"} to keep
its gate without its accuracy: word's accuracy is excluded from
individual-differences inference anyway, on reliability grounds.}
}
\value{
A \code{brmsprior} object. Print it to see every prior.
}
\description{
Companion to \code{\link[=joint_formula]{joint_formula()}}, and must be built with the same
arguments or brms will reject priors naming coefficients the model does
not contain.

\code{lkj(4)} rather than the \code{lkj(2)} used on smaller models here: 15
correlations estimated from 86 participants need more regularisation
than 3 do. It puts a marginal SD of about 0.28 on each correlation, so a
correlation has to be earned rather than assumed. The marginal prior on
any single correlation under \code{lkj(eta)} in \code{d} dimensions is
\code{Beta(a, a)} rescaled to (-1, 1) with \code{a = eta + (d - 2) / 2}
(Lewandowski, Kurowicka & Joe, 2009), which is what
\code{\link[=lkj_marginal]{lkj_marginal()}} returns.
}
