% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_scores.R
\name{score_angular}
\alias{score_angular}
\title{Angular similarity via cosine}
\usage{
score_angular(target, response, period)
}
\arguments{
\item{target, response}{Numeric vectors of angles in degrees.}

\item{period}{Circumference of the angular space, in degrees.}
}
\value{
Numeric vector in [0, 1].
}
\description{
\code{similarity = (cos(2 * pi * error / period) + 1) / 2}, where \code{error} is
the circular distance between target and response on a circle of
circumference \code{period}. Returns 1 for a perfect match and 0 for a
maximally wrong answer, using the full [0, 1] range.
}
\section{Choice of period}{

The two angular features have \strong{different periods}, and were initially
treated as one case:
\itemize{
\item \strong{Colour} is a hue wheel: \code{period = 360}. Red at 5° and red at 355°
are 10° apart, not 350°.
\item \strong{Orientation} is the tilt of a plain rectangle, which has 180°
rotational symmetry: \code{period = 180}. A rectangle at +80° and one at
-80° differ by 160° arithmetically but are only 20° apart as
orientations, and both look near-horizontal. The response widget
clamps to [-90, 90] (\code{normalizeAngle()} in \code{utils.js}), which is
exactly one period.
}

Using \code{period = 360} for orientation would treat a 90° error —
perpendicular, the most wrong an orientation can be — as similarity
0.5 rather than 0, and would compress the observed range to
[0.13, 1]. See \code{\link[=compute_scores]{compute_scores()}}'s \code{orientation_period} argument;
this remains flagged for confirmation against real distributions.
}

