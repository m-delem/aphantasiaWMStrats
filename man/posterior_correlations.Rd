% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_helpers.R
\name{posterior_correlations}
\alias{posterior_correlations}
\title{Participant-level correlations from a multivariate model}
\usage{
posterior_correlations(draws, group = "id", lkj = 4, dimension = NULL)
}
\arguments{
\item{draws}{A \code{draws_df}, or anything with the correlation columns,
typically \code{brms::as_draws_df(model)}.}

\item{group}{The grouping factor name used in the model.}

\item{lkj}{The LKJ shape parameter the model was fitted with, used for
the \code{moved} column. \code{NULL} skips that column.}

\item{dimension}{Size of the correlation matrix, for the same purpose.}
}
\value{
A tibble with one row per correlation: the two responses, the
median, a 95\% interval, the probability of direction, and \code{moved}.
}
\description{
Pulls the group-level correlation parameters out of a fitted model and
returns them tidily, with the response names recovered from the
parameter names rather than assumed. brms names these
\verb{cor_<group>__<a>_Intercept__<b>_Intercept}, and the order of \code{a} and
\code{b} follows the order the responses appear in the formula, so a caller
asking for a specific pair cannot know in advance which way round it is.

Also reports whether each posterior is narrower than the marginal prior
on a single correlation under \code{lkj(eta)} (see \code{\link[=lkj_marginal]{lkj_marginal()}}). With
fifteen correlations estimated from fewer than a hundred participants,
some will not have moved, and a correlation that did not move is not a
finding.
}
