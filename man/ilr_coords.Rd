% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{ilr_coords}
\alias{ilr_coords}
\title{Isometric log-ratio coordinates for a three-part composition}
\usage{
ilr_coords(parts, first = c("word", "color", "angle"))
}
\arguments{
\item{parts}{A data frame or matrix with the three closed parts, named
either \code{part_word}/\code{part_angle}/\code{part_color} or \code{word}/\code{angle}/\code{color}.}

\item{first}{The feature contrasted against the other two in the first
coordinate. One of \code{"word"} (default), \code{"color"}, \code{"angle"}.}
}
\value{
A tibble with \code{ilr1} and \code{ilr2}, and attributes \code{first} and
\code{balance} recording which partition produced them.
}
\description{
The balanced ILR transform, with the sequential binary partition (SBP)
given explicitly by \code{first}. This choice belongs to the researcher and
should not be made on statistical grounds: picking whichever partition
maximises a contrast selects a split nobody has a substantive interest
in. It is therefore an explicit argument with a default matching the
study's own decision, rather than a hidden constant.

Convention, identical to the one used in \verb{inst/scripts/06a-reliability.R},
so that the split-half stability estimates computed there apply to these
coordinates unchanged:

\deqn{ilr_1 = \sqrt{2/3} \log(p_1 / \sqrt{p_2 p_3})}
\deqn{ilr_2 = \sqrt{1/2} \log(p_2 / p_3)}

where \eqn{p_1} is \code{first} and \eqn{p_2, p_3} are the remaining two parts
in the canonical order \verb{word, color, angle}. For the default
(\code{first = "word"}), \code{ilr1} is verbal versus non-verbal allocation and
\code{ilr2} is colour versus orientation.

Any two-coordinate ILR basis is a rotation of the same geometry: the
omnibus test, the Aitchison distances and the total variance do not
depend on \code{first}. What depends on it is which single-coordinate claim
can be stated.
}
