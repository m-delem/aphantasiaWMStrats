% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_helpers.R
\name{split_half_reliability}
\alias{split_half_reliability}
\title{Split-half reliability of a per-feature score}
\usage{
split_half_reliability(
  data,
  feature,
  n_splits = 1000,
  thresholds = wm_thresholds(),
  trial_col = "trial_uid"
)
}
\arguments{
\item{data}{Item-level data for one version, test blocks only.}

\item{feature}{One of \code{"word"}, \code{"angle"}, \code{"color"}.}

\item{n_splits}{Number of random splits.}

\item{thresholds}{Minimum responded items per feature, defaulting to
\code{\link[=wm_thresholds]{wm_thresholds()}}.}

\item{trial_col}{Name of the column identifying a trial.}
}
\value{
A numeric vector of \code{n_splits} half-test correlations, on the
raw scale. Apply \code{\link[=spearman_brown]{spearman_brown()}} to get full-test reliability.
}
\description{
Splits at the \strong{trial} level rather than by odd and even items, because
items within a trial share a memory load: an odd-even split would
correlate two halves of the same trial and overstate reliability.

Participants are included per feature, on their own responded-item
count, since someone can be measurable on colour and not on orientation.
}
