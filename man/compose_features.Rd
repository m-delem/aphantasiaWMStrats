% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/composition.R
\name{compose_features}
\alias{compose_features}
\title{Responders-only compositional parts}
\usage{
compose_features(data, ..., features = c("word", "angle", "color"))
}
\arguments{
\item{data}{Item-level data.}

\item{...}{Grouping columns, tidy-evaluated (e.g. \code{id}, or \verb{id, trial_uid}).}

\item{features}{Feature stems, in the canonical order used throughout the
package.}
}
\value{
A tibble with the grouping columns, \code{n_word}/\code{n_angle}/\code{n_color}
(responded items contributing to each part), the raw responders-only
means \verb{mean_*}, and the closed parts \verb{part_*} summing to 1. Groups
missing a part entirely are returned with \code{NA} parts rather than
dropped, so the caller decides what to do with them.
}
\description{
Builds the three-part composition (word, orientation, colour) from
\strong{responded trials only}. Scores of 0 recorded for non-responses do not
measure allocation, they measure which features a participant declined.
That is a separate quantity, and mixing the two would make the
composition partly an index of who answered what. Non-response rates
differ by feature and, in the pooled data, by imagery group.

Grouping is left to the caller through \code{...}, which is what lets the same
function build participant-level compositions (\code{id}) and trial-level ones
(\code{id}, \code{trial_uid}).
}
