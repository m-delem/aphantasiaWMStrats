% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_floor_group.R
\name{plot_vviq_histogram}
\alias{plot_vviq_histogram}
\title{Marginal histogram of imagery vividness}
\usage{
plot_vviq_histogram(
  vviq,
  floor_x = 16,
  binwidth = 2,
  floor_fill = "#C44E52",
  floor_colour = "#8B3A3E",
  floor_alpha = 0.5,
  gradient = ggplot2::scale_fill_viridis_c(option = "viridis", guide = "none"),
  left_expansion = 0.09,
  base_size = 7
)
}
\arguments{
\item{vviq}{A numeric vector of vividness scores.}

\item{floor_x}{The scale minimum, drawn as its own bar.}

\item{binwidth}{Histogram bin width for the above-floor bars. The floor
bar is drawn separately and is not affected by it.}

\item{floor_fill, floor_colour, floor_alpha}{Fill, outline and opacity of
the floor bar. Match them to \code{\link[=plot_floor_group]{plot_floor_group()}}'s violin so stacked
panels read as one figure.}

\item{gradient}{Continuous fill scale for the above-floor bars, coloured
by bin midpoint, or \code{NULL} for a plain fill.}

\item{left_expansion}{Fraction of the x range added on the left. Share
it with \code{\link[=plot_floor_group]{plot_floor_group()}} so stacked panels align.}

\item{base_size}{Passed to \code{\link[=theme_pdf]{theme_pdf()}}.}
}
\value{
A ggplot object with the x axis stripped, for stacking.
}
\description{
The piece of data that motivates the floor-group model on its own: the
vividness distribution is not smoothly continuous, but a sharp isolated
spike at the scale floor plus an irregular remainder above it. Ported
from \code{aphantasiaEmotions}, where it sits above the main panel.

Stacked above \code{\link[=plot_floor_group]{plot_floor_group()}} with \code{patchwork}, sharing an x axis,
it makes the case for the model before the model is shown.

Bins are computed directly and drawn as two \code{geom_col()} layers rather
than by \code{geom_histogram()} with a fill aesthetic. Mapping fill to a
floor indicator stacks two groups inside whichever bin straddles the
boundary, which colours part of the floor bar as above-floor.
}
