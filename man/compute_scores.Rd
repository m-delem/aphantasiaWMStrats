% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_scores.R
\name{compute_scores}
\alias{compute_scores}
\title{Compute WM-FTT recall scores}
\usage{
compute_scores(
  data,
  orientation_period = 180,
  colour_period = 360,
  no_response_angle = 90,
  no_response_colour = 999
)
}
\arguments{
\item{data}{A stimulus-level data frame, e.g. \link{all_data}.}

\item{orientation_period, colour_period}{Angular periods in degrees. See
\code{\link[=score_angular]{score_angular()}} for why they differ.}

\item{no_response_angle}{Sentinel marking an untouched orientation
widget.}

\item{no_response_colour}{Sentinel marking an untouched colour wheel.}
}
\value{
\code{data} with the columns above appended.
}
\description{
Adds raw and per-version-standardised similarity scores for the three
recalled features, plus response indicators, to a stimulus-level frame
such as \link{all_data}. Scores are computed from raw target/response values
rather than derived from the front end's \verb{live_diff_*} columns, which
are display-only and, for the word feature, not a valid edit distance
(see \link{all_data}).
}
\section{Columns added}{

\itemize{
\item \code{score_word}, \code{score_angle}, \code{score_color} — raw similarity in
[0, 1], higher is better. Note the direction is \strong{opposite} to the
\verb{live_diff_*} columns, which are dissimilarities.
\item \code{score_word_z}, \code{score_angle_z}, \code{score_color_z} — the above,
z-scored within \code{version}. Both are kept: raw for anything needing
absolute performance, standardised for cross-feature comparison and
clustering. Note that standardised scores cannot be used for any
log-ratio transform, since roughly half of them are negative.
\item \code{responded_word}, \code{responded_angle}, \code{responded_color} — logical.
\item \code{responded_parity_1}, \code{responded_parity_2} — logical, added when the
\verb{parity_*_resp} columns are present. The parity accuracy columns use
the same score-a-non-response-as-zero convention as recall, and in v1
92\% of their zeros are unanswered probes rather than errors, so any
analysis of parity needs these flags. See \code{\link[=flag_parity_responses]{flag_parity_responses()}}.
}
}

\section{Non-responses}{

The task encodes non-response with per-feature sentinels rather than
\code{NA}: an empty \code{response_word}, a \code{response_color_angle} of 999, and a
\code{response_angle} of exactly 90 (the orientation widget's untouched
starting position). These are scored \strong{0} and flagged, rather than set
to \code{NA}, because non-response here is very unlikely to be
missing-at-random — a participant who skips colour is plausibly
reporting the absence of a colour representation, and \code{NA} would let
listwise deletion silently drop exactly those observations. Scoring 0
produces a visible distortion in the distribution instead of an
invisible one in the inference.

\code{responded_angle} is an \strong{inference} (\code{response_angle != 90}), not an
observation, unlike the other two. Max \code{target_angle} is 61°, and no
block trial has a target within 20° of 90°, so a 90° response is never
near-correct — but a genuine deliberate 90° cannot be distinguished
from an untouched slider.
}

\section{Standardisation moments}{

Means and SDs are computed on \strong{experimental-block rows only}
(\code{expe_phase} matching \verb{^expe_block}) and then applied to every row, so
that tutorial and training rows are scored on the same scale without
contributing to it. Tutorial rows carry hardcoded placeholder values in
the front end's own columns; their target/response values are real, so
they are scored here, but they should be filtered before any aggregate.
}

